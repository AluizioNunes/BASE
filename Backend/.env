# Arquivo de exemplo para variáveis de ambiente.
# Copie este arquivo para um novo arquivo chamado .env e preencha com seus valores locais.

# URL de conexão para o banco de dados PostgreSQL
# Formato: postgresql://usuario:senha@localhost:5432/nome_do_banco
DATABASE_URL="postgresql://BASE:BASE@10.10.255.111:5432/BASE"

# Nível de log do SQLAlchemy (opcional, útil para debug)
# Valores possíveis: DEBUG, INFO, WARNING, ERROR, CRITICAL
SQLALCHEMY_LOG_LEVEL=INFO

# Chave secreta para geração de tokens JWT (altere para um valor seguro em produção)
SECRET_KEY="BASE"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30
